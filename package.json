{
  "name": "github-dev-dashboard-extension",
  "displayName": "Dev Dashboard for GitHub",
  "description": "A Control Center for your Github project in VS Code",
  "version": "0.0.1",
  "publisher": "local-dev",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Dev Dashboard",
      "properties": {
        "devDash.debug": {
          "type": "boolean",
          "default": false,
          "description": "Enable debug logging for the GitHub Issues & PRs extension"
        }
      }
    },
    "commands": [
      {
        "command": "devDash.openViewer",
        "title": "DevDash Open",
        "category": "Dev Dashboard Open",
        "icon": "$(github)"
      },
      {
        "command": "devDash.showDebugChannel",
        "title": "Show Debug Output",
        "category": "GitHub Viewer"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "devDash.openViewer",
          "when": "resourceScheme == file",
          "group": "navigation"
        }
      ],
      "commandPalette": [
        {
          "command": "devDash.openViewer",
          "when": "workspaceFolderCount > 0"
        },
        {
          "command": "devDash.showDebugChannel"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/marked": "^5.0.2",
    "@types/node": "18.x",
    "@types/vscode": "^1.74.0",
    "@typescript-eslint/eslint-plugin": "^6.4.1",
    "@typescript-eslint/parser": "^6.4.1",
    "@vscode/test-cli": "^0.0.9",
    "@vscode/test-electron": "^2.3.4",
    "eslint": "^8.47.0",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@octokit/rest": "^19.0.7",
    "marked": "^16.1.2"
  }
}
